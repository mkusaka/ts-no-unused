{
  "name": "ts-no-unused",
  "version": "0.0.1",
  "repository": "https://github.com/mkusaka/ts-no-unused.git",
  "author": "mkusaka <hinoshita1992@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.4.5",
    "prettier": "^2.3.2",
    "ts-node": "^10.1.0",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "meow": "^10.1.0",
    "ts-morph": "^11.0.3"
  },
  "type": "module",
  "bin": {
    "ts-no-unused": "dist/cli.js"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "prepublishOnly": "yarn build && git push origin HEAD && git push --tags"
  }
}
